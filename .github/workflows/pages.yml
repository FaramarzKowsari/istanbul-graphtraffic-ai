name: Pages sanity check
on:
  push:
    branches: [main]
    paths: ["docs/**"]
  workflow_dispatch:
jobs:
  validate:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Verify site entry point
        run: test -f docs/index.html && test -f docs/assets/site.css
